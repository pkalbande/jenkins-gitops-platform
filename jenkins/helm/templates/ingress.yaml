{{- if .Values.jenkins.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.jenkins.name }}-ingress
  namespace: {{ .Values.jenkins.namespace }}
  annotations:
    {{- range $key, $value := .Values.jenkins.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.jenkins.ingress.className }}
  {{- if .Values.jenkins.ingress.tls }}
  tls:
    {{- range .Values.jenkins.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.jenkins.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $.Values.jenkins.name }}
                port:
                  number: {{ .port }}
          {{- end }}
    {{- end }}
{{- end }}
